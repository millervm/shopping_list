<h1>Edit List</h1>

<%= render 'form' %>
<br></br>
<%= button_to "Delete List", "/lists/#{@list.id}", method: :delete, data: { confirm: 'Are you sure you want to delete this list and all of its contents?' } %>
<br></br>
<%= link_to "Cancel", list_path(@list) %>